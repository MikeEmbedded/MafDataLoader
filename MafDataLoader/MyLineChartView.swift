//
//  MyLineChartView.swift
//  MafDataLoader
//
//  Created by Mihail Trosinenco on 10.12.2021.
//

import UIKit
import Charts
/*
open class MyyyyLineChartView: LineChartView {
    internal func initialize1() {
        super.initialize()
        let pan = UIPanGestureRecognizer(target: self, action: #selector(lineChartGestureRecognizer))
        self.addGestureRecognizer(pan)
    }
    
    @objc func lineChartGestureRecognizer(_ recognizer: UIPanGestureRecognizer) {
        switch recognizer.state {
        case .began:
            print("pann began")
            
            if let marker = self.marker as? MarkerView {
                let location = recognizer.location(in: self)
            }
        case .changed:
            print("pann changed")
        default:
            print("pann default")
        }
        
    }

}
*/
